[project]
name = "compression-comparison-range-coding-vs-deflate"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "argparse>=1.4.0",
    "bitarray>=3.8.1",
    "logging>=0.4.9.6",
    "numpy>=2.2.6",
]
